trigger:
- main

pool:
  vmImage: ubuntu-latest

stages:
- stage: Build
  jobs:
  - job: FirstJob
    steps:
    - bash: echo build FirstJob
    - bash: echo $(pipelinevariablelevel)
    - bash: echo $(Build.BuildNumber)
    - bash: echo $(Build.BuildID)
    - bash: echo $(Build.SourceBranchName)
    - bash: echo $(Build.SourcesDirectory)
    - bash: echo $(System.DefaultWorkingDirectory)
    - bash: ls -R $(System.DefaultWorkingDirectory)
    - bash: echo $(Build.ArtificatStagingDirectory)
    - bash: java -version
    - bash: node --version
    - bash: python --version
    - bash: mvn -version
    - bash: ls -R $(Build.ArtificatStagingDirectory)
    - task: CopyFiles@2
      inputs:
        SourceFolder: '$(System.DefaultWorkingDirectory)'
        Contents: |
          **/*.yaml
          **/*.tf
        TargetFolder: '$(Build.ArtificatStagingDirectory)'
    - bash: ls -R $(Build.ArtificatStagingDirectory)
    - task: PublishBuildArtifacts@1
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)'
        ArtifactName: 'drop'
        publishLocation: 'Container'
        MaxArtifactSize: 1
#   - job: SecondJob
#     steps:
#     - bash: echo build SecondJob
# - stage: DevDeploy
#   variables:
#    environment: Dev
#   dependsOn: Build
#   jobs:
#   - job: DevDeployJob
#     steps:
#     - bash: echo build $(environment)DeployJob
# - stage: QADeploy
#   variables:
#     environment: QA
#   dependsOn: DevDeploy
#   jobs:
#   - job: QADeployJob
#     steps:
#     - bash: echo build $(environment)DeployJob
# - stage: PRODDeploy
#   variables:
#     environment: PROD
#   dependsOn: 
#   - DevDeploy
#   - QADeploy
#   jobs:
#   - job: PRODDeployJob
#     steps:
#     - bash: echo build $(PROD)DeployJob